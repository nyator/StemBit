apply plugin: 'com.android.library'
apply plugin: 'expo-module-gradle-plugin'

group = 'expo.modules.audioroutes'
version = '1.0.0'

android {
  namespace "expo.modules.audioroutes"
  defaultConfig {
    versionCode 1
    versionName "1.0.0"
  }
}
