# Copy to .env and fill in. Never commit .env.
# Appwrite dev key (Appwrite console -> Project -> Settings -> Dev keys).
# Only needed in development; leave empty for production builds.
EXPO_PUBLIC_APPWRITE_DEV_KEY=
